import React from "react";

function Note() {
    return ( <
        div className = "note" >
        <
        h1 id = "note h1" > Note Title < /h1> <
        p id = "note p" > Note content < /p> <
        /div>
    );
}

export default Note;
